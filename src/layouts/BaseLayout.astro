---
import "../styles/global.css";

interface Props {
  title: string;
  description?: string;
  ogImage?: string;
}

const {
  title,
  description = "Brave AI Community Szczecin — spolecznosc AI w Szczecinie. Meetupy, prezentacje i networking.",
  ogImage = "/images/og-image.png",
} = Astro.props;

const siteBase = Astro.site ?? "https://braveai.community";
const canonicalUrl = new URL(Astro.url.pathname, siteBase);
const ogImageUrl = new URL(ogImage, siteBase).href;
---

<!doctype html>
<html lang="pl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="canonical" href={canonicalUrl.href} />

    <!-- Favicon -->
    <link rel="icon" type="image/svg+xml" href="/favicon/favicon.svg" />
    <link rel="icon" type="image/png" sizes="96x96" href="/favicon/favicon-96x96.png" />
    <link rel="icon" href="/favicon/favicon.ico" sizes="48x48" />
    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png" />
    <link rel="manifest" href="/favicon/site.webmanifest" />

    <!-- Open Graph -->
    <meta property="og:title" content={`${title} | Brave AI Community Szczecin`} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImageUrl} />
    <meta property="og:type" content="website" />
    <meta property="og:locale" content="pl_PL" />

    <title>{title} | Brave AI Community Szczecin</title>
  </head>
  <body
    class="bg-surface-primary text-text-primary font-archivo min-h-screen flex flex-col"
  >
    <slot name="navbar" />
    <main class="flex-1">
      <slot />
    </main>
    <slot name="footer" />
  </body>
</html>
